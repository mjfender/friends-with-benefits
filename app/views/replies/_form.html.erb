<%= form_for @reply do |f| %>

  <%= f.label :comment %><br>
  <%= f.text_area :comment %><br><br>

  <%= f.hidden_field :need_id, :value => @need.id %>

  <%= f.submit "Reply", class: "#{default_button_class}" %>
<% end %>
