<%= form_for @reply do |f| %>
  <%= f.label "I'm in!" %><br>
  <%= f.check_box :yes, :checked => true %><br><br>

  <%= f.label :comment %><br>
  <%= f.text_area :comment %><br><br>

  <%= f.hidden_field :need_id, :value => @need.id %>

  <%= f.submit %>
<% end %>
