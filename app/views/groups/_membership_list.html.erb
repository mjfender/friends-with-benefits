<% if @group.member?(@user) %>

  <h3> Members:</h3>
  <%= @group.members.count %> members strong | <%= link_to "Leave Group", leave_group_path(@group) %>
  <div>
  <% @group.members.each do |member| %>

   <%= profile_pic(member) %>
   <% if @group.admin?(@user) && @group.closed? %>
   <%= link_to 'x', revoke_membership_path(@group, member) %>
   <% end %>

   <% end %>
   </div>
<% else %>
<h3> Members: </h3>
  <%= @group.member_count %> happy members!
<% end %>